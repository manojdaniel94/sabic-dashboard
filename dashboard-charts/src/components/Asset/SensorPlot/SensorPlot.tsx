import React, { useRef } from "react";
import "./SensorPlot.scss"

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const SensorPlot = () => {
    return (
        <button className="btn">

        </button>

    );
};

export default SensorPlot;