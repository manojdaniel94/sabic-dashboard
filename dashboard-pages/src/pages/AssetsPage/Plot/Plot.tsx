import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const Plot = () => {
    return (
        <div>
            Plot
        </div>

    );
};

export default Plot;