import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const LiveTracking = () => {
    return (
        <div>
            LiveTracking
        </div>

    );
};

export default LiveTracking;

