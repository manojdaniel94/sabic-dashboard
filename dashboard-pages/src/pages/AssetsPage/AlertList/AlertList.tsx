import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const AlertList = () => {
    return (
        <div>
            AlertList
        </div>

    );
};

export default AlertList;