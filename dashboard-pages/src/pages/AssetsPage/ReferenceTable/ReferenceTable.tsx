import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const ReferenceTable = () => {
    return (
        <div>
            ReferenceTable
        </div>

    );
};

export default ReferenceTable;