import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const AssetTimeLine = () => {
    return (
        <div>
            AssetTimeLine
        </div>

    );
};

export default AssetTimeLine;