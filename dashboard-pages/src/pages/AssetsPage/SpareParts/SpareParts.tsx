import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}

const SpareParts = () => {
    return (
        <div>
            SpareParts
        </div>

    );
};

export default SpareParts;

