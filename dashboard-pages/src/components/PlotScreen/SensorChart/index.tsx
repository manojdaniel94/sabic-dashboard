import React, { useRef } from "react";

interface Props {
    buttonName: string;
    onClickHandler: any;
}
const SensorChart = () => {
    return (
        <div className="sensor-plot-graph"></div>
    );
};

export default SensorChart;